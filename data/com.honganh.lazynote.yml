# Flatpak manifest (starting point — validate with flatpak-builder on a real box).
#
#   flatpak install flathub org.kde.Platform//6.8 org.kde.Sdk//6.8
#   flatpak-builder --user --install --force-clean build-dir data/com.honganh.lazynote.yml
#   flatpak run com.honganh.lazynote
#
# The KDE runtime supplies Qt 6. PySide6 + python-xlib are installed into the app
# via pip during the build (pinned wheels should be added under python3-modules for
# a fully offline/reproducible build).
app-id: com.honganh.lazynote
runtime: org.kde.Platform
runtime-version: "6.8"
sdk: org.kde.Sdk
command: lazynote
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --talk-name=org.freedesktop.portal.GlobalShortcuts
  - --filesystem=xdg-config/lazynote:create
  # Read-only access to import notes from the old Electron/Tauri builds on first launch:
  - --filesystem=xdg-config/Antinote:ro
modules:
  - name: lazynote
    buildsystem: simple
    # `pip3 install` reaches PyPI, so the build sandbox needs network access.
    # Fine for a self-distributed bundle; for Flathub, vendor pinned wheels as
    # python3-modules sources instead and drop this.
    build-options:
      build-args:
        - --share=network
    build-commands:
      - pip3 install --prefix=/app --no-build-isolation .
      - install -Dm644 data/com.honganh.lazynote.desktop /app/share/applications/com.honganh.lazynote.desktop
      - install -Dm644 data/com.honganh.lazynote.metainfo.xml /app/share/metainfo/com.honganh.lazynote.metainfo.xml
      - install -Dm644 src/lazynote/icon.png /app/share/icons/hicolor/512x512/apps/com.honganh.lazynote.png
    sources:
      - type: dir
        path: ..
